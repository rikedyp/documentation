services:
  mkdocs-server:
    build:
      context: .
      dockerfile: mkdocs/Dockerfile  # Dockerfile for serving MkDocs site
    volumes:
      - ${DOCS_DIR}:/docs
    ports:
      - "8000:8000"
    environment:
      - DOCS_DIR=${DOCS_DIR}
    working_dir: /docs